package com.ab.design;

/**
 * @author Arpit Bhardwaj
 *
 * Document Based (Documents could be a JSON Model)
 * Database contains collection (one or multiple) which further contain documents
 * We feed JSON but behind the scene it converts it to BSON and feed in db
 * Schemaless (No/Few Relations)
 * Distributed,
 * Provides CP from CAP Theorem
 * Implemented using c++
 *
 * Use case:
 *
 *
 */