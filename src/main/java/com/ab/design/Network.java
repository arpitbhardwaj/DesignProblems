package com.ab.design;

/**
 * @author Arpit Bhardwaj
 *
 * TCP vs UDP
 *
 * HTTP vs HTTPs
 *      HTTP is unsecured while HTTPS is secured.
 *      HTTP operates at application layer, while HTTPS operates transport layer.
 *      SSL Certificates is required in HTTPs for domain validation which is not the case oin HTTP
 *      No data encryption in HTTP, with HTTPS the data is encrypted before sending.
 */